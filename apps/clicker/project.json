{
  "name": "clicker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/clicker/src",
  "targets": {
    "build": {
      "command": "npm run build",
      "options": {
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/build"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "command": "npm run lint",
      "options": {
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "command": "npm run test",
      "options": {
        "cwd": "{projectRoot}"
      }
    },
    "validate": {
      "command": "echo ok",
      "options": {
        "cwd": "{projectRoot}"
      }
    },
    "dev": {
      "command": "npm run dev",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  }
}
